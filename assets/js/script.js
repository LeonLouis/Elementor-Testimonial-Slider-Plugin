jQuery( document ).ready( function( $ ){

  $('.cbl-ets-wrapper').slick({
    dots: true,
    infinite: true,
    speed: 500,
    fade: true,
    cssEase: 'linear',
    adaptiveHeight: true,
    arrows: false
  });

});